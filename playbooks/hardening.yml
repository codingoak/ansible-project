###############################################################
# RHEL 10.1 Hardening Playbook
# Goal: CIS-oriented baseline hardening for managed nodes
# Author: Learning project
###############################################################

- name: RHEL 10.1 Server Hardening
  hosts: managed
  become: true

  ###############################################################
  # Variables
  ###############################################################
  vars:
    # SSH settings
    ssh_port: 22
    ssh_idle_timeout: 300
    ssh_max_auth_tries: 3

    # Password policies
    password_min_length: 12
    password_max_days: 90

    # Kernel networking
    # Default: disabled for hardening
    # Override for Kubernetes nodes via group_vars
    kernel_ip_forward: "0"

    # Packages that must NOT be installed
    packages_to_remove:
      - telnet
      - rsh
      - rsh-server
      - ypbind
      - ypserv
      - tftp
      - tftp-server

    # Services that should be disabled
    services_to_disable:
      - bluetooth
      - avahi-daemon
      - cups

  ###############################################################
  # BLOCK 1: System updates
  ###############################################################
  tasks:
    - name: "[1/7] Placeholder - system update tasks"
      ansible.builtin.debug:
        msg: "System update block – not implemented yet"
      tags: update

  ###############################################################
  # BLOCK 2: Remove insecure packages
  ###############################################################
    - name: "[2/7] Placeholder - package removal"
      ansible.builtin.debug:
        msg: "Package hardening block – not implemented yet"
      tags: packages

  ###############################################################
  # BLOCK 3: SELinux configuration
  ###############################################################
    - name: "[3/7] Placeholder - SELinux configuration"
      ansible.builtin.debug:
        msg: "SELinux hardening block – not implemented yet"
      tags: selinux

  ###############################################################
  # BLOCK 4: SSH hardening
  ###############################################################
    - name: "[4/7] Placeholder - SSH hardening"
      ansible.builtin.debug:
        msg: "SSH hardening block – not implemented yet"
      tags: ssh

  ###############################################################
  # BLOCK 5: Firewall configuration
  ###############################################################
    - name: "[5/7] Placeholder - firewall configuration"
      ansible.builtin.debug:
        msg: "Firewall hardening block – not implemented yet"
      tags: firewall

  ###############################################################
  # BLOCK 6: Audit logging
  ###############################################################
    - name: "[6/7] Placeholder - audit logging"
      ansible.builtin.debug:
        msg: "Audit logging block – not implemented yet"
      tags: audit

  ###############################################################
  # BLOCK 7: Password policy & kernel hardening
  ###############################################################
    - name: "[7/7] Placeholder - password & kernel hardening"
      ansible.builtin.debug:
        msg: "Password and kernel hardening block – not implemented yet"
      tags:
        - password
        - sysctl
        - services

  ###############################################################
  # HANDLERS (defined early, implemented later)
  ###############################################################
  handlers:
    - name: restart sshd
      ansible.builtin.debug:
        msg: "sshd restart handler placeholder"

    - name: reload auditd rules
      ansible.builtin.debug:
        msg: "auditd rules reload handler placeholder"
